# 用户表

user 系统用户

- id
- username
- password_hash
- created_at

扩展登录功能

# 游戏账号表

game_account 游戏账号

- id
- user_id
- game_uid  UNIQUE
- server_id
- nickname（待扩展）
- created_at（待扩展）

user 1 -> n game_account

# 游戏资源表

game_resource 游戏资源

- id
- resource_id  UNIQUE
- resource_name
- resource_type
- quality_level

# 卡池表

gacha_pool 卡池

- id
- pool_code
- pool_name
- pool_type

# 抽卡记录表（只读）

api_gacha_record

- id
- account_id
- pool_id
- resource_id（外键）

- business_key

- import_task_id

- draw_time  业务时间，实际抽取时间
- in_second_seq

- api_draw_order（经常查询）  计算得到的顺序

- source_type（API，MANUAL）

- created_at
- updated_at

舍弃is_up，计算代价很低，改为动态计算。

构造业务唯一键，作为去重依据。

business_key:

UNIQUE(uid + pool_id + resource_id + draw_time + in_second_seq)

# 手动导入数据表（允许编辑）

manual_gacha_segment

- id
- account_id

- source_type

- pool_type

- resource_name（五星名称）
- resource_id（匹配id）

- pity_count（消耗抽数）

- manual_draw_order

- insert_after_record_id（插入位置）

- created_at
- updated_at

# 导入任务表

import_task

- id
- account_id
- source_type  记录来源

- status

- request_record_id
- card_pool_id

- imported_count

- created_at

# 一条抽卡记录的生命周期

官方api 返回--解析--生成business_key--查重--不存在--插入数据库--后处理计算