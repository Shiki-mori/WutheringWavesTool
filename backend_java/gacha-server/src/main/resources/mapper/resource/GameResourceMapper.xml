<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.phrolova.gacha.server.mapper.GameResourceMapper">

    <select id="selectByResourceId" resultType="com.phrolova.gacha.pojo.entity.GameResource">
        select id, resource_id as resourceId, resource_name as resourceName,
               resource_type as resourceType, quality_level as qualityLevel
        from game_resource
        where resource_id = #{resourceId}
    </select>

    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        insert into game_resource (resource_id, resource_name, resource_type, quality_level)
        values (#{resourceId}, #{resourceName}, #{resourceType}, #{qualityLevel})
    </insert>
</mapper>
